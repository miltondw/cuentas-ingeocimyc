import type { Service } from "@/types/admin";
import type { ColumnConfig, RowAction } from "@/components/common/DataTable";
import type { FilterField } from "@/components/common/SearchAndFilter";

// Si necesitas tipos locales, agrégalos aquí. Si ya existen, simplemente reexporta o importa.
export type { Service, ColumnConfig, RowAction, FilterField };
