import { ProjectFormCorrected } from "../components/ProjectFormCorrected";

/**
 * Página para crear/editar proyectos
 * Usa el componente corregido basado en la estructura real del backend
 */
export const CreateProjectPageCorrected = () => {
  return <ProjectFormCorrected />;
};

export default CreateProjectPageCorrected;
